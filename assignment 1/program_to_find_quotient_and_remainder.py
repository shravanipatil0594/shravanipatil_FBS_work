# program to find quotient and remainder
num=float(input('enter a first value : '))
num2=float(input('enter a second value : '))

quotient = num // num2
remainder = num % num2 

print(f'quotient is : {quotient} & remainder is : {remainder}')